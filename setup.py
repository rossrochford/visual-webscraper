from setuptools import setup

# to install run: python setup.py install

setup(
    name="visual-webscraper",
    version="0.0.01",
    author="Ross Rochford",
    packages=['visual_webscraper'],
    classifiers=[],
)
